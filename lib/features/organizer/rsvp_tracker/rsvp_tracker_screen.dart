import 'package:flutter/material.dart';

class RsvpTrackerScreen extends StatelessWidget {
  const RsvpTrackerScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('RSVP Tracker')),
      body: const Center(
        // TODO: implement — player list with confirmed/pending status and Avatar widgets
        child: Text('TODO: implement'),
      ),
    );
  }
}
