import 'package:flutter/material.dart';

class PaymentLedgerScreen extends StatelessWidget {
  const PaymentLedgerScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Payment Ledger')),
      body: const Center(
        // TODO: implement — payment list with StatusPill widgets and mark-paid action
        child: Text('TODO: implement'),
      ),
    );
  }
}
